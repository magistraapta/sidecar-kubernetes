apiVersion: v1
kind: Service
metadata:
  name: go-app-service
  namespace: go-kubernetes
spec:
  type: ClusterIP
  selector:
    app: go-app
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
    name: http
